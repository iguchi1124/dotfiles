interface:
  display_name: "Orchestrator"
  short_description: "Plan, implement, and independently evaluate changes"
  default_prompt: "Use $orchestrator to run this task through the appropriate staged workflow."

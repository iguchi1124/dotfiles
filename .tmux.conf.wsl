bind -T copy-mode-vi Enter send-keys -X copy-pipe "cat | clip.exe"
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "cat | clip.exe"
bind -T copy-mode-vi Y send-keys -X copy-pipe "cat | clip.exe"

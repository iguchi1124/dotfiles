interface:
  display_name: "Orchestrator"
  short_description: "Execute one task with isolated plan, build, and check roles"
  default_prompt: "Use $orchestrator to execute this task with separate planning, implementation, and evaluation contexts."

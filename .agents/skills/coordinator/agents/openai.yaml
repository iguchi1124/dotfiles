interface:
  display_name: "Coordinator"
  short_description: "Coordinate parallel tasks with durable shared state"
  default_prompt: "Use $coordinator to organize this multi-task project, persist its specification and task graph, and coordinate safe parallel work."

interface:
  display_name: "Codex Setup"
  short_description: "Install and verify this repository's Codex setup"
  default_prompt: "Use $codex-setup to install or verify the Codex configuration from this dotfiles repository."

interface:
  display_name: "Coordinator"
  short_description: "Manage shared tasks, ownership, and project progress"
  default_prompt: "Use $coordinator to create or update the shared tasks.md, assign worktrees, and track this project's progress."

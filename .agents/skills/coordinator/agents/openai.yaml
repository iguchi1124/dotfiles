interface:
  display_name: "Coordinator"
  short_description: "Manage shared tasks, ownership, and project progress"
  default_prompt: "Use $coordinator to prepare task IDs and update the shared project board. Show which tasks are ready for separate execution."
